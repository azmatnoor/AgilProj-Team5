@model RestaurantReview

<div class="card mt-1 mb-1">
    <div class="card-header">
        <h4>@Model.Author</h4>
    </div>
    <div class="card-body">
        <p>@Model.Message</p>
    </div>
</div>